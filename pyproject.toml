[project]
name = "netherite"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gymnasium",
    "numpy",
    "opencv-python>=4.13.0.92",
    "posix-ipc>=1.3.2",
    "pygame>=2.6.1",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]
